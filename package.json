{
  "name": "battle-ax",
  "version": "0.0.1",
  "description": "vuex wrapper for type-safe",
  "keywords": [ "typescript", "type safe", "vue", "vuex" ],
  "main": "dist/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "npm run build:tsc && npm run build:rollup",
    "build:rollup": "rollup -c",
    "build:tsc": "tsc",
    "clean": "rm -rf dist lib",
    "prepublish": "npm run clean && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "https://github.com/joe-re/battle-ax",
  "bugs": "https://github.com/joe-re/battle-ax/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/joe-re/battle-ax.git"
  },
  "files": [ "dist", "lib" ],
  "author": "joe-re",
  "license": "MIT",
  "devDependencies": {
    "rollup": "^0.45.2",
    "rollup-plugin-typescript2": "^0.4.6",
    "typescript": "^2.4.2",
    "vue": "^2.4.2",
    "vuex": "^2.3.1"
  }
}
